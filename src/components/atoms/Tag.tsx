import React from "react";

const Tag = () => {
  return (
    <span className="uppercase bg-accent-green p-1 font-semibold rounded text-sm absolute top-[-20px]">
      Most popular
    </span>
  );
};

export default Tag;
